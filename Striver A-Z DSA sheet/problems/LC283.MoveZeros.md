# Move Zeroes

## Problem
Given an integer array nums, move all 0's to the end of it while maintaining the relative order of the non-zero elements.

Note that you must do this in-place without making a copy of the array.

## Approach Bf
select the non zero element and put it in the temp arraylist.fill the original array using values from the arraylist at front and put zero at end.


## Code bf
```java
(class Solution {
    public void moveZeroes(int[] nums) {
       int size = nums.length;
       List<Integer> list = new ArrayList<>();

       for(int i =0;i<size;i++){
             if(nums[i]!=0){
                list.add(nums[i]);
             }
       }
       for(int i =0;i<size;i++){
            if(i<list.size()){
                nums[i]=list.get(i);
            }
            else{
                nums[i]=0;
            }
       }
    }
}) 


## Approach OP
set j pointer at the zero and i pointer next to j,comapare the both pointer if i is non zero swap with j and then increment j , other wise i pointer only get incremented



## Code bf
class Solution {
    public void moveZeroes(int[] nums) {
            int j=0;
            for(int i =0;i<nums.length;i++){
            if(nums[i]!=0){
                int temp = nums[j];
                nums[j] = nums[i];
                nums[i] = temp;
                j++;
            }
            }
 }
}